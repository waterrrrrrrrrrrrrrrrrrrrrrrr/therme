<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Portal Admin Login — <%= appName %></title>
  <link rel="stylesheet" href="/app.css">
  <style>
    body{display:flex;align-items:center;justify-content:center;min-height:100vh;background:radial-gradient(ellipse at top,#1c1f26 0%,#0f1115 60%,#0b0d11 100%);}
    .card{background:linear-gradient(180deg,#1f2328 0%,#181b20 100%);padding:36px;border-radius:16px;max-width:380px;width:100%;box-shadow:0 20px 50px rgba(0,0,0,.7);}
    .badge{display:inline-block;background:#7c3aed22;color:#a78bfa;border:1px solid #7c3aed44;padding:4px 10px;border-radius:20px;font-size:12px;font-weight:600;margin-bottom:20px;}
    h2{margin:0 0 6px;font-size:22px;}
    p{color:#9ca3af;font-size:13px;margin:0 0 24px;}
    label{display:block;font-size:13px;color:#9ca3af;margin-bottom:6px;}
    input{width:100%;box-sizing:border-box;background:#0f1115;border:1.5px solid #2d3748;color:#fff;padding:12px 14px;border-radius:10px;font-size:15px;outline:none;margin-bottom:14px;}
    input:focus{border-color:#7c3aed;}
    .btn{width:100%;padding:13px;background:linear-gradient(180deg,#7c3aed,#6d28d9);color:#fff;border:none;border-radius:10px;font-size:15px;font-weight:600;cursor:pointer;}
    .error{background:#1a0f1f;border:1px solid #ef4444;color:#f87171;padding:10px 14px;border-radius:8px;margin-bottom:16px;font-size:14px;}
  </style>
</head>
<body>
<div class="card">
  <span class="badge">Portal Admin</span>
  <h2>Portal Admin Login</h2>
  <p>This area is restricted to portal administrators only.</p>
  <% if (error) { %><div class="error"><%= error %></div><% } %>
  <form method="POST" action="/portal/login">
    <input type="hidden" name="_security-token" value="<%= security-tokenToken %>">
    <label>Username</label>
    <input type="text" name="username" required autofocus autocomplete="username">
    <label>Password</label>
    <input type="password" name="password" required autocomplete="current-password">
    <button class="btn">Sign In</button>
  </form>
</div>
</body>
</html>
