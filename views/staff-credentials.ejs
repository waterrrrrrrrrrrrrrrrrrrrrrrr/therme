<!DOCTYPE html>
<html>
<head>
  <title>Staff Credentials</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/app.css">
  <style>
    body { background: #0d1117; color: #e6edf3; font-family: Arial, sans-serif; margin: 0; }
    .creds-container {
      max-width: 480px;
      margin: 80px auto;
      padding: 0 20px;
    }
    .creds-card {
      background: #161b22;
      border: 1px solid #30363d;
      border-left: 5px solid #22c55e;
      border-radius: 12px;
      padding: 32px;
    }
    .creds-icon { font-size: 2.5rem; margin-bottom: 16px; }
    .creds-title { font-size: 1.4rem; font-weight: 700; margin: 0 0 8px; }
    .creds-subtitle { color: #8b949e; margin: 0 0 28px; font-size: 0.95rem; }
    .cred-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 0;
      border-bottom: 1px solid #21262d;
    }
    .cred-row:last-of-type { border-bottom: none; }
    .cred-label { color: #8b949e; font-size: 0.9rem; }
    .cred-value {
      font-family: monospace;
      font-size: 1rem;
      font-weight: 700;
      color: #e6edf3;
      background: #0d1117;
      padding: 4px 10px;
      border-radius: 6px;
      border: 1px solid #30363d;
    }
    .warning-box {
      margin-top: 24px;
      background: #1c1a07;
      border: 1px solid #9e6a03;
      border-radius: 8px;
      padding: 12px 16px;
      font-size: 0.875rem;
      color: #d29922;
    }
    .warning-box strong { display: block; margin-bottom: 4px; }
    .btn-back {
      display: inline-block;
      margin-top: 24px;
      padding: 10px 20px;
      background: linear-gradient(180deg, #3b82f6, #2563eb);
      color: white;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.95rem;
    }
    .btn-back:hover { opacity: 0.9; }
  </style>
</head>
<body>
  <div class="creds-container">
    <div class="creds-card">
      <div class="creds-icon">✅</div>
      <h1 class="creds-title">Staff Member Created</h1>
      <p class="creds-subtitle">Share these credentials with the new team member.</p>

      <% if (creds) { %>
      <div class="cred-row">
        <span class="cred-label">Name</span>
        <span class="cred-value"><%= creds.name %></span>
      </div>
      <div class="cred-row">
        <span class="cred-label">Username</span>
        <span class="cred-value"><%= creds.username %></span>
      </div>
      <% if (creds.password) { %>
      <div class="cred-row">
        <span class="cred-label">Temp Password</span>
        <span class="cred-value"><%= creds.password %></span>
      </div>
      <% } %>
      <% } else { %>
      <p style="color:#6b7280;text-align:center;">No credentials to display. They may have already been viewed.</p>
      <% } %>

      <div class="warning-box">
        <strong>Important – Save these credentials now.</strong>
        This page will not be shown again. The password cannot be recovered after you leave.
      </div>

      <a href="/app/staff" class="btn-back">← Back to Staff</a>
    </div>
  </div>
</body>
</html>
